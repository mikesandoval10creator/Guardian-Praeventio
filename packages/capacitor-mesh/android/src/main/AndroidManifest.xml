<?xml version="1.0" encoding="utf-8"?>
<!--
  SPDX-License-Identifier: MIT
  @praeventio/capacitor-mesh — Android manifest (Sprint 30 SCAFFOLD).

  Permissions match the BLE GATT advertising/scanning + Wi-Fi Direct
  fallback the runtime will need in Sprint 31. Declared up-front so the
  permission flow can be wired and tested before the native code lands.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth LE — Android 12+ runtime permissions. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31"
        xmlns:tools="http://schemas.android.com/tools" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="31"
        xmlns:tools="http://schemas.android.com/tools" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31"
        xmlns:tools="http://schemas.android.com/tools" />

    <!-- Legacy BLE permissions for SDK <= 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Required for BLE scanning on SDK <= 30 (companion-device flow). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Wi-Fi Direct fallback for chunks > 100 KB (Sprint 32). -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33"
        xmlns:tools="http://schemas.android.com/tools" />

    <!-- Foreground service for long-running mesh during emergencies. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:targetApi="34"
        xmlns:tools="http://schemas.android.com/tools" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
</manifest>
