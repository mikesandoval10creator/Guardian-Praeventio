# Third-party notices

This package derives its compatibility API and native screen behaviour from
`@capgo/capacitor-proximity` 8.1.9:

- Project: https://github.com/Cap-go/capacitor-proximity
- npm package metadata license: MIT
- Retrieved dependency version: 8.1.9

Praeventio's local implementation adds JavaScript `near`/`far` events,
`getCurrent`, lifecycle ownership, invalid-reading guards, tests, and release
evidence requirements. This notice is retained so the origin and divergence are
auditable even though the external package is no longer installed at runtime.
