// Vitest setup file (loaded for every test, both node and jsdom environments).
// Add jest-dom matchers here once the dep is available
// (i.e. once `@testing-library/jest-dom` is installed as a devDependency:
//   import '@testing-library/jest-dom/vitest';
// — for now this file is intentionally minimal so tests parse without
// requiring an unlisted dep).
export {};
