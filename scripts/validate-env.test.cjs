// Sprint 21 Ola 6 / Bucket U.6 — pointer file.
//
// This file is named the way the bucket spec asked, but vitest is
// configured to ONLY discover tests under `src/**/*.test.ts(x)`
// (see vitest.config.ts). The actual test suite lives at:
//
//   src/__tests__/scripts/validateEnv.test.ts
//
// To run just the validate-env tests:
//   npx vitest run src/__tests__/scripts/validateEnv.test.ts
//
// Keeping this file as a discoverability anchor so future contributors
// who `ls scripts/` see immediately where the tests are.
'use strict';

module.exports = {
  testFile: '../src/__tests__/scripts/validateEnv.test.ts',
};
