# DEEP-EX-26 — B11 Contratistas/Visitas/Conducción/Proveedores (FEAT slice) · Lote #26

**Deriva:** `ledger.json` filtrado `category` empieza con `FEAT` && `block === "B11-Contratistas"`, ordenado por `path`, slice `[0:35]` (los 35).
**Atestación:** 35/35 archivos leídos completos, línea por línea. `file:line` reales.
**Método:** code-first. No repito lo ya cubierto por `DEEP-B11-Contratistas.md` (visitors.ts sin assertProjectMember; stack `visitorAccessService`/`VisitorCheckInForm`/`useActiveVisitors` muerto; `resolveObservation` sin consumer; DS76 duplicado). Foco en hallazgos NUEVOS.

---

## 1. Resumen de hallazgos nuevos

| Archivo:línea | Sev | Hallazgo | Evidencia |
|---|---|---|---|
| `src/App.tsx:274` + `:279` vs `src/routes/OperationsRoutes.tsx:47` | 🔴 | **Colisión de ruta `safe-driving`.** El path `safe-driving` se declara DOS veces en el mismo `<Routes>`: vía spread `{OperationsRoutes}` (App.tsx:274 → `<SafeDriving/>`, OperationsRoutes.tsx:47) y de nuevo inline (App.tsx:279 → `<SafeDrivingMode/>`). React Router resuelve al primero declarado → `/safe-driving` renderiza **SafeDriving.tsx** y **SafeDrivingMode.tsx queda sombreado/muerto** en esa ruta (sólo alcanzable por navegación imperativa que igualmente colisiona). Mismo patrón duplicado en el árbol alterno (App.tsx:446 vs OperationsRoutes mount :439). | grep confirma 3 `path="safe-driving"`, OperationsRoutes montado en :274/:439 antes del inline :279/:446 |
| `src/pages/SafeDriving.tsx:94-102` | 🔴 | **Escritura cliente a colección sin regla → default-deny (rota en prod).** `addDoc(collection(db, 'projects/{id}/driving_incidents'), …)` escribe a `driving_incidents`, que **no tiene match en `firestore.rules`** (sólo existe `driving_reports`, regla en firestore.rules:647). En prod la escritura es rechazada por default-deny; el doc nunca persiste y el reporte de incidente vial se pierde silenciosamente (el `catch` llama `handleFirestoreError` y `return`, sin avisar al usuario más allá de un log). Además no escribe `userId` ni pasa por audit_logs (#3) — es write client-side directo. | grep: `NO RULE for driving_incidents`; SafeDriving.tsx:94 set sin userId |
| `src/pages/ClimateRoutes.tsx:215` | 🔴 | **Botón "Calcular Ruta Óptima" no calcula nada — sólo cicla el semáforo (cosmético).** El `onClick` hace `setRouteStatus(safe→warning→danger→safe)`, NO llama `calculateRoute()` ni `assessRouteClimate`. El usuario cree que recalcula la ruta/clima pero sólo rota un color falso; el assessment real sólo corre en el `useEffect` de carga inicial (:158) o al cambiar origin/destination indirectamente. Engaña sobre la frescura del análisis NASA/EONET. | ClimateRoutes.tsx:215 `onClick={() => setRouteStatus(...)}` |
| `src/services/visitors/visitorFirestoreAdapter.ts:11-12` | 🟡 | **Colección `visitor_accesses` sin regla en `firestore.rules` (default-deny).** El adapter escribe/lee `tenants/{tid}/projects/{pid}/visitor_accesses`. No hay match en rules (grep: NONE). Es parte del stack paralelo muerto, pero si alguien lo cableara fallaría en prod por default-deny. Confirma que el stack no fue probado end-to-end contra rules. | grep `visitor_accesses` en firestore.rules → NONE |
| `src/pages/SafeDriving.tsx:389-392`, `:404-408` | 🟡 | **Botones sin handler (UI muerta).** "Adjuntar Foto" (:389) y "Llamar a Central de Emergencias" (:404) no tienen `onClick` — son placeholders visuales que no hacen nada. El usuario en emergencia vial podría tocar "Llamar a Central" esperando una llamada. | SafeDriving.tsx:389/404 sin `onClick` |
| `src/pages/SafeDriving.tsx:281-336` | 🟡 | **Panel de logística hardcoded/ficticio.** "Tiempo Estimado 1h 45m", "Checklist Vehículo: Completado", "Destino: Planta Industrial Norte", alerta "lluvia ligera km 45" son strings fijos no derivados de ningún dato. El checklist `preDriverChecklist` (:50) se muestra siempre como completado sin que el conductor lo haya marcado. | SafeDriving.tsx:179,:290,:301,:332 literales |
| `src/hooks/useDrivingSafety.ts:7` · `src/hooks/useSuppliers.ts:6` | 🟡 | **Import `auth` muerto.** Ambos hooks importan `{ auth } from '../services/firebase'` pero NO lo usan (0 referencias `auth.` en cada archivo; usan `apiAuthHeader`/`useEndpoint`). Dead import — ruido + posible bundle weight. | grep `auth\.` → 0 en ambos |
| `src/hooks/useRouteScoring.ts` · `useRouting.ts` · `useClimateAwareScheduling.ts` · `useConsultativeSale.ts` · `useContractors.ts` · `useActiveVisitors.ts` | 🔵 | **6 hooks/clientes 100% huérfanos (backend listo, frontend sin cablear).** Ningún consumer fuera de su propia definición (grep `NONE` para `useRouteScoring`, `useRouting`, `useClimateAwareScheduling`, `useConsultativeSale`, `useContractors`, `useActiveVisitors` y sus exports `assessTaskWeatherRemote`/`buildClimateAwareDailyPlan`/`findPathAStarRemote`/`buildRouteProfile`). Amplía la deuda "backend-first" ya señalada (antes sólo contractors/consultativeSale/geofence/vendor). | grep consumers → NONE |
| `src/components/ContractorRankingTable.tsx` · `SupplierComparator.tsx` · `DriverScoreCard.tsx` · `ClimatePlanAdjustment.tsx` · `VisitorCheckInForm.tsx` | 🔵 | **5 componentes "Wire UI #N" huérfanos.** Ninguno se importa fuera de su propio archivo/test (grep NONE para los 5). Todos llaman motores puros client-side (`rankContractorsByRisk`, `rankSuppliers`, `computeDriverScore`, `buildDailyPlanAdjustment`, `validateCheckIn`) en vez de los endpoints remotos correspondientes. Tests verdes sobre UI no montada. | grep importers → NONE |
| `src/pages/MiningContractors.tsx:33,:84` | 🟡 | **No persiste (confirmación, ya señalado en B11).** `emptyContractor()` seed local; el comentario de cabecera (:7-10) promete `projects/{id}/miningContractors` pero el page nunca escribe ahí (grep `miningContractors` en rules → NONE; no hay `setDoc`/`addDoc`). Genera PDF real pero los datos no sobreviven recarga. Lo dejo como 🟡 de contexto, no nuevo. | MiningContractors.tsx sin write Firestore |
| `src/server/routes/suppliers.ts:431,:489` | 🔵 | **ID con `db.collection('_ids').doc().id` (patrón inusual).** Genera ID de incident/audit creando un docRef descartado en una colección fantasma `_ids`. Funciona (Firestore genera ID sin escribir) pero es un anti-patrón vs `randomId()`/`crypto.randomUUID()` usado en el resto del repo. No viola #15 técnicamente (no es `Math.random`) pero es inconsistente. | suppliers.ts:431,:489 |
| `src/pages/Visitors.tsx:97` | 🔵 | **`Math.random()` en Idempotency-Key client-side.** `newIdempotencyKey` usa `Math.random().toString(36)`. #15 prohíbe `Math.random` en `src/server/` e ID-gen; esto es cliente (fuera de scope estricto) y es key de idempotencia, no ID persistido, pero el `prefix-Date.now()-random` podría colisionar bajo reloj congelado en tests. Bajo. | Visitors.tsx:97 |

---

## 2. Lo que está BIEN (verificado, limpio)

- **`src/server/routes/visitors.ts`** — `hostUid`/`tenantId` siempre del token (`:116`, `tenantIdFor` :78); check-out (:202) y acknowledge-induction (:270) en `runTransaction` (cumple #19); 3 mutadores `await auditServerEvent` (:158,:230,:305) (cumple #3/#14); IDs `vis_<ts>_<uuid>` con `randomUUID()` (:103, cumple #15). **(La falta de `assertProjectMember` ya está documentada en DEEP-B11 — sigue presente, no la re-listo como nueva.)**
- **`src/server/routes/suppliers.ts`** — `guard()` → `assertProjectMember` + `resolveTenantId` del proyecto (`:64-88`); `registeredByUid`/`recordedByUid` del token (`:379,:435,:495`, nunca del body); audit `await` en create/incident/audit (`:384,:444,:504`); 5xx genérico `internal_error` (cumple #8); `tenantId` NUNCA del body (#tenant). `riskLevel` validado por Zod (:298). Read-modify-write de incidents/audits hace `get()`+`set(merge)` **sin transacción** pero es append a array embebido (no lost-update sobre el mismo campo escalar) — borderline #19, aceptable.
- **`src/server/routes/contractors.ts`** y **`consultativeSale.ts`** — compute puro, `verifyAuth` + `guard()`/`assertProjectMember` en cada endpoint (contractors.ts:35; consultativeSale.ts:37); 5xx genérico; sin escritura Firestore (sin audit necesario). Limpios.
- **Motores puros** — `contractorKpiService.ts` (TRIR/LTIFR/severity con constantes 200k/1M, `buildAcreditationGapReport` :160), `supplierScoring.ts` (pesos 40/30/20/10, `validate` rechaza no-finitos :94 — Codex hardening), `supplierQualityService.ts` (SLA + sole-supplier audit), `consultativeSalePlaybook.ts`, `visitorRegistry.ts` (event-sourcing, RUT por forma :122) — todos deterministas, sin I/O, sin LLM, sin diagnóstico. Cumplen #9.
- **`Driving.tsx`** — shell de mapa + speedómetro + SOS `tel:` deshabilitado si no hay phone (:159, evita link muerto); banner wildfire EONET con cache offline (:103) y `dismiss`; touch targets ≥44px documentados. Limpio (los botones del dock son stubs-toast pero documentados :16-20 y el persistence layer es SafeDriving por diseño).
- **`SafeDrivingMode.tsx`** — `driving_reports` write con `userId` del token (:29-34) y la regla existe (firestore.rules:647, create exige `userId == auth.uid`); SOS vía `triggerEmergency` (contexto), `tel:` deshabilitado sin phone (:151). Web Speech API local (sin red). Limpio salvo el sombreado de ruta (ver 🔴 colisión).
- **`DrivingSafety.tsx`** + **`useDrivingSafety.ts`** — page cableada a endpoints reales `/api/sprint-k/.../driving/*` (router montado server.ts:1030); modales register/flag-alert/journey con validación y `logger`; no bloquea operación (directiva #2). Sólo el import `auth` está muerto.
- **`SupplierQuality.tsx`** + **`useSuppliers.ts`** — page cableada a `/api/sprint-k/.../suppliers*` (montado server.ts:1023); register/list/detalle reales; sólo `auth` import muerto.
- **`QRScannerModal.tsx`** — limpio; `scanner.clear()` con `.catch` (:39). Usado por Visitors.tsx (inducción).

---

## 3. Para decisión del usuario

- 🔴 **Colisión `safe-driving`:** ¿`SafeDrivingMode` o `SafeDriving` debe servir `/safe-driving`? Hoy gana SafeDriving (OperationsRoutes, montado primero) y SafeDrivingMode queda muerto en esa ruta pese a estar lazy-importado y referenciado 2x en App.tsx. Decidir cuál es canónico y eliminar el duplicado.
- 🔴 **`driving_incidents` sin regla:** o agregar rule (create con `userId`+`projectId`+`isProjectMember`, ≥5 tests, Dirty Dozen) o migrar el reporte de SafeDriving al endpoint server-side. Hoy el reporte de incidente vial se pierde en prod.
- 🔴 **Botón ClimateRoutes:** cablear "Calcular Ruta Óptima" a `calculateRoute()`/`assessRouteClimate` o eliminarlo. Hoy miente sobre el análisis.
- 🔵 **Backend B11 sin frontend:** 6 hooks + 5 componentes "Wire UI" listos y testeados pero ningún page los monta. ¿Sprint de cableado UI o retiro del código muerto? Arrastra tests verdes sobre código no ejecutable end-to-end.
- 🟡 **SafeDriving.tsx:** panel de logística ficticio + 2 botones sin handler. ¿Cablear o marcar como demo?

_Doc-only. Sin git commit._
