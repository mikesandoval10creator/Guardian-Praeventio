<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug variant override for the Android launch APK.

  The release-source network_security_config.xml carries the documented
  production pin placeholders so the CI wiring test and release ratchet can
  enforce certificate pinning. Android parses the selected resource before the
  Application starts, so placeholders must never be selected for debug builds:
  they are not valid base64 SHA-256 digests and would crash startup.

  Replace the placeholders in the main/release resource with real SPKI pins
  before producing a release build. This debug resource deliberately has no
  pin-set and is never used by the Play release variant.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">app.praeventio.net</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
